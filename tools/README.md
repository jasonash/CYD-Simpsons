# Tools

- `make_test_media.sh` generates a synthetic test AVI (frame counter overlay, 440 Hz tone) in the playback format. Copy it to the SD card root as `bench.bin` or any `.avi` for the Phase 0 spike.

Planned: flashing helper, SD card layout checker.
